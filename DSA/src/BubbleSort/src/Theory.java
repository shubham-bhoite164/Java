public class Theory {
    public static void main(String[] args) {
        
        // In every step you are comparing the adjacent elements.
        // If the next element is greater than current element then we are going to swap it.
        // And keep on doing it.
        
        
        // Time Complexity is= ( Best case O(N) )  ( Worst case O(N) )
        // space complexity is O(1), we don't need any extra space

    }
}
